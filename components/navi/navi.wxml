<!--components/navi/navi.wxml-->
<view class='container'>
  <image class="icon" src="{{first ? disLeftSrc : leftSrc}}" />
  <text class="title">{{title}}</text>
  <image class="icon" src="{{latest ? disRightSrc :rightSrc}}" />
</view>
